spring:
  datasource:
    url: jdbc:mysql://database.cwrv58p0ko6g.ap-northeast-2.rds.amazonaws.com:3306/jpa_shop
    username: admin
    password: rownsdn91#
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
#      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql

logging:
  level:
    #    org.hibernate.SQL: debug
    org.hibernage.type: trace
